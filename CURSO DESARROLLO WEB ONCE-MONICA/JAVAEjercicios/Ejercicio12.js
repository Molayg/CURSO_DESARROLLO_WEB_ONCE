

//Calcular promedio de strings: Crea una función que reciba por parámetro un array y cuando es un valor number lo sume y de lo
//contrario cuente la longitud del string y lo sume. Puedes usar este array para probar tu función:

const mixedElements = [6, 1, 'Rayo', 1, 'vallecano', '10', 'upgrade', 8, 'hub'];
function averageWord(param) {
    function averageWord(param)
    let total = 0;
    for(const elemente of param){
        // Si el tipo de dato del elemento es un string...
        
        if (typeof element === "string"){
            // Le sumamos al total la longitud del string
            // En el cas de Rayo, el .length es 4
            titak + elemente.length;
} else {
    // Si no es un string se sumam normal 
  total = element;
       }
       const quantity = param.length;
       console.log(total/ quantity);
}


}

