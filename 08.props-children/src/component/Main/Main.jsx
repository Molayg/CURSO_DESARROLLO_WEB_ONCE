import "./Main.css";
import videogames from "../../data/videogames";
import Card from "../Card/Card";

const Main = () => {
    return (
        <main>
           <ul>
             {videogames.map((videogame) => (
               ((videogame) => (
                <Card data =
               )
         )
                <li key = {videogame.id}>
            )
         }
        </ul>
        </main>
    )

}))